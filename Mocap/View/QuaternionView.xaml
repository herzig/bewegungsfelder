<UserControl x:Class="Mocap.View.QuaternionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mocap.View"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="300"
             x:Name="Root"
             >
    <DockPanel VerticalAlignment="Center" LastChildFill="True">
        <TextBlock DockPanel.Dock="Left" Text="Angle:" />
        <TextBox  DockPanel.Dock="Left" Text="{Binding Angle, ElementName=Root}" Width="50" />
        <TextBlock Text="Axis:" />
        <TextBox Text="{Binding Axis, ElementName=Root}" MinWidth="60" />
    </DockPanel>
</UserControl>
